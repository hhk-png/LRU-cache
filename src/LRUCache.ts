
console.log('maxSizeArray.ts')

export class maxSizeArray<T> {
  maxSize: number
  arr: T[]

  constructor(maxSize: number, array: T[]) {
    this.maxSize = 10
    this.arr = []
  }

  push(item: T) {

  }
  
}
